import React from "react";
import styles from "./aboutme.module.scss";

function AboutMe() {
  return (
    <div id="aboutme" className={`${styles.aboutMe} max-w-5xl mx-auto`}>
      AboutMe
    </div>
  );
}

export default AboutMe;
